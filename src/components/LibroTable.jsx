import { FiEdit2, FiTrash2, FiBook } from "react-icons/fi";
function LibroTable({ libros, onEditar, onEliminar }) {
  if (libros.length === 0) return <p className="empty-state">No existen libros registrados.</p>;
  return <div className="table-card"><div className="table-card__heading"><div><span><FiBook /></span><div><h3>Lista de libros</h3><p>{libros.length} registros encontrados</p></div></div></div><div className="table-scroll"><table><thead><tr><th>ID</th><th>Título</th><th>Autor</th><th>ISBN</th><th>Categoría</th><th>Stock</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{libros.map((libro) => <tr key={libro.id}><td>#{libro.id}</td><td><strong>{libro.titulo}</strong></td><td>{libro.autor}</td><td>{libro.isbn}</td><td>{libro.categoria ? libro.categoria.nombre : `Categoría ID ${libro.categoria_id}`}</td><td><strong className="stock-value">{libro.stock}</strong></td><td><span className={`badge badge--${libro.estado?.toLowerCase()}`}>{libro.estado}</span></td><td className="table-actions"><button type="button" className="icon-button icon-button--edit" onClick={() => onEditar(libro)} aria-label="Editar libro"><FiEdit2 /><span>Editar</span></button><button type="button" className="icon-button icon-button--delete" onClick={() => onEliminar(libro.id)} aria-label="Eliminar libro"><FiTrash2 /><span>Eliminar</span></button></td></tr>)}</tbody></table></div></div>;
}
export default LibroTable;
