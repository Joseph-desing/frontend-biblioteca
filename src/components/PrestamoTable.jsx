import { FiEdit2, FiTrash2, FiRepeat } from "react-icons/fi";
function PrestamoTable({ prestamos, onEditar, onEliminar }) {
  if (prestamos.length === 0) return <p className="empty-state">No existen préstamos registrados.</p>;
  return <div className="table-card"><div className="table-card__heading"><div><span><FiRepeat /></span><div><h3>Lista de préstamos</h3><p>{prestamos.length} registros encontrados</p></div></div></div><div className="table-scroll"><table><thead><tr><th>ID</th><th>Usuario</th><th>Libro</th><th>Fecha préstamo</th><th>Fecha devolución</th><th>Estado</th><th>Observaciones</th><th>Acciones</th></tr></thead><tbody>{prestamos.map((prestamo) => <tr key={prestamo.id}><td>#{prestamo.id}</td><td><strong>{prestamo.usuario ? prestamo.usuario.nombre : `Usuario ID ${prestamo.usuario_id}`}</strong></td><td>{prestamo.libro ? `${prestamo.libro.titulo} - ${prestamo.libro.autor}` : `Libro ID ${prestamo.libro_id}`}</td><td>{prestamo.fecha_prestamo}</td><td>{prestamo.fecha_devolucion}</td><td><span className={`badge badge--${prestamo.estado?.toLowerCase()}`}>{prestamo.estado}</span></td><td>{prestamo.observaciones || "Sin observaciones"}</td><td className="table-actions"><button type="button" className="icon-button icon-button--edit" onClick={() => onEditar(prestamo)} aria-label="Editar préstamo"><FiEdit2 /><span>Editar</span></button><button type="button" className="icon-button icon-button--delete" onClick={() => onEliminar(prestamo.id)} aria-label="Eliminar préstamo"><FiTrash2 /><span>Eliminar</span></button></td></tr>)}</tbody></table></div></div>;
}
export default PrestamoTable;
